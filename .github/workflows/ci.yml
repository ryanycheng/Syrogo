name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test-build-lint:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Run tests
        run: go test ./...

      - name: Run installer contract tests
        run: bash -n scripts/install.sh && bash -n scripts/install_test.sh && bash scripts/install_test.sh

      - name: Build binary
        run: go build ./cmd/syrogo

      - name: Run golangci-lint
        uses: golangci/golangci-lint-action@v7
        with:
          version: v2.11.4
